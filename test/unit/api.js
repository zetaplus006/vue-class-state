import { expect } from 'chai';
import * as Vubx from '../';
console.log(Object.keys(Vubx).sort());
describe('api', function () {
    it('export apis', function () {
        expect(Object.keys(Vubx).sort()).to.equal([

        ])
    })
})